Do not modify any code.

I have added two new Secrets in Replit:

- OPENCODE_API_KEY
- NVIDIA_NIM_API_KEY

First, verify that both secrets are present and correctly loaded.

For each secret, report:

1. Secret Name
2. Present (Yes/No)
3. Loaded Successfully (Yes/No)
4. Environment Variable Accessible (Yes/No)
5. Provider Initialized (Yes/No)
6. Any Error Message

Do NOT reveal the actual API key values.

--------------------------------------------------

NVIDIA NIM Test

Perform a live API connection test.

Send this prompt:

Reply with exactly one word:

OK

Report:

- Model Used
- HTTP Status
- Success/Failed
- Response Time
- Response
- Cost (if available)
- Error (if any)

Verify whether NVIDIA NIM supports:

- Chat Completions
- Script Analysis
- Structured JSON Output
- Function Calling (if supported)
- Streaming

--------------------------------------------------

OpenCode Test

Perform a live API connection test.

Send this prompt:

Reply with exactly one word:

OK

Report:

- HTTP Status
- Success/Failed
- Response Time
- Provider/Model Used
- Response
- Error (if any)

Also verify whether OpenCode can be used for:

- Script Analysis
- Chat Completions
- JSON Output

--------------------------------------------------

Compatibility Check

Verify whether both providers can be integrated into the current AI pipeline.

If supported, recommend this AI priority:

1. NVIDIA NIM
2. OpenCode
3. OpenRouter (FREE models only)
4. Gemini
5. Rule-based fallback

The application must automatically retry the next provider if one fails.

--------------------------------------------------

Finally provide this report:

Environment Variables

✅ OPENCODE_API_KEY
✅ NVIDIA_NIM_API_KEY

Connection Status

✅ NVIDIA NIM Working
✅ OpenCode Working

OR

❌ Provider Name
Exact Reason

Do not modify any code.
Only inspect the current configuration, perform live API tests, and provide a complete diagnostic report.